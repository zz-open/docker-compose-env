volumes:
  flink-cdc:
    driver: ${VOLUMES_DRIVER}

services:

### Flink-CDC ################################################
  flink-cdc:
    build:
      context: ${FLINK_CDC_HOST_PATH}
      args:
        DOCKER_REGISTRY_URL: ${DOCKER_REGISTRY_URL}
        APACHE_DORIS_VERSION: ${APACHE_DORIS_VERSION}
    container_name: ${FLINK_CDC_CONTAINER_NAME}
    hostname: ${FLINK_CDC_HOSTNAME}
    restart: ${DOCKER_RESTART_POLICY}
    environment:
      TZ: ${TZ}
    volumes:
      - apache-doris-fe-data:/opt/apache-doris/fe/doris-meta
    ports:
      - 8030:8030
    networks:
      backend:
        ipv4_address: ${FLINK_CDC_BACKEND_IP}
